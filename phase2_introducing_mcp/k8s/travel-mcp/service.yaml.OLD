---
apiVersion: v1
kind: Service
metadata:
  name: travel-mcp
  namespace: demo-travel
  labels:
    app: travel-mcp
spec:
  type: NodePort
  selector:
    app: travel-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30100
